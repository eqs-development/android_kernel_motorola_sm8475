ifeq ($(CONFIG_BUILD_ARM64_DT_OVERLAY),y)
ifeq ($(CONFIG_MMI_DEVICE_DTBS),y)
ifeq ($(CONFIG_EQS_DTB),y)

dtbo-$(CONFIG_ARCH_CAPE) += \
	eqs-0.dtbo \
	eqs-1.dtbo \
	eqs-2.dtbo \
	eqs-3.dtbo \
	eqs-4.dtbo \
	eqs-5.dtbo \
	eqs-6.dtbo \
	eqs-7.dtbo

eqs-0.dtbo-base := cape.dtb
eqs-1.dtbo-base := cape.dtb
eqs-2.dtbo-base := cape.dtb
eqs-3.dtbo-base := cape.dtb
eqs-4.dtbo-base := cape.dtb
eqs-5.dtbo-base := cape.dtb
eqs-6.dtbo-base := cape.dtb
eqs-7.dtbo-base := cape.dtb

endif
endif
endif
